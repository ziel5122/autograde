import React, { Component } from 'react';

class ClassHome extends Component {
  constructor(props) {
    super(props);
    this.setState({
      code: props.code,
      name: props.name,
    })


  }

  render() {
    return(

    );
  }
}
